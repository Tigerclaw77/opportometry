# --- Runtime ---
NODE_ENV=development
PORT=4242
APP_URL=http://localhost:5173   # your React (Vite) dev URL
CORS_ORIGIN=http://localhost:5173

# --- MongoDB ---
MONGO_URI=mongodb+srv://USERNAME:PASSWORD@YOURCLUSTER.mongodb.net/JOBS_VISION?retryWrites=true&w=majority

# --- Auth ---
JWT_SECRET=change_me_before_prod

# --- Stripe ---
# Get from Dashboard → Developers → API keys (test mode while developing)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxx
# For local dev, run: stripe listen --forward-to http://localhost:4242/api/stripe/webhook
# and paste the printed whsec_... here:
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxx
